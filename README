IEEE-manager is a chrome extension which manages articles downloaded from IEEE xplore website.When you try to download an article it parse the webpage get the title of article , suthors ,date of publishing etc and add it to filename .Now instead of some silly numbers as filenames you can now clearly recognize your article .Moreover it downloads all the articles in a separate directory.
